name: xnz_image
description: A Flutter component package with a demo app for network image loading and caching-ready UI behavior.
version: 0.1.3
homepage: https://github.com/qqxnz/xnz_image.git
repository: https://github.com/qqxnz/xnz_image.git
platforms:
  android:
  ios:
  linux:
  macos:
  windows:

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.8.0
  path_provider: ^2.1.5
  flutter_avif_platform_interface: ^3.1.0
  flutter_avif_android: ^3.1.0
  flutter_avif_ios: ^3.1.0
  flutter_avif_macos: ^3.1.0
  flutter_avif_linux: ^3.1.0
  flutter_avif_windows: ^3.1.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
