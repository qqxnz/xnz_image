name: xnz_net_cache_image
description: A Flutter component package with a demo app for network image loading and caching-ready UI behavior.
version: 0.1.0
homepage: https://example.com/xnz_net_cache_image

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:

